<views:MvxPhonePage
    x:Class="ValueConversion.UI.WP.Views.ColorsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="clr-namespace:Cirrious.MvvmCross.WindowsPhone.Views;assembly=Cirrious.MvvmCross.WindowsPhone"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel Grid.Row="0" Margin="12,17,0,28">
            <TextBlock Text="MY APPLICATION" Style="{StaticResource PhoneTextNormalStyle}"/>
            <TextBlock Text="Colors" Margin="9,-7,0,0" Style="{StaticResource PhoneTextTitle1Style}"/>
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="12,0,12,0">
            <StackPanel>
    <Slider 
        Minimum="0"
        Maximum="255"
        Value="{Binding Red, Mode=TwoWay}"
        Background="Red"
        Foreground="DeepPink"
        ></Slider>
                <Slider 
        Minimum="0"
        Maximum="255"
        Value="{Binding Green, Mode=TwoWay}"
        Background="Green"
        Foreground="Lime"
        ></Slider>
                <Slider 
        Minimum="0"
        Maximum="255"
        Value="{Binding Blue, Mode=TwoWay}"
        Background="Blue"
        Foreground="Cyan"
        ></Slider>
               <Grid>
                    <Rectangle
                    Height="120"
                    Fill="{Binding Color, Converter={StaticResource NativeColor}}"
                    ></Rectangle>
                    <TextBlock
                    Text="{Binding Color}"
                        Foreground="{Binding Color, Converter={StaticResource ContrastColor}}"
                    ></TextBlock>
                </Grid>
            </StackPanel>
        </Grid>
    </Grid>

</views:MvxPhonePage>