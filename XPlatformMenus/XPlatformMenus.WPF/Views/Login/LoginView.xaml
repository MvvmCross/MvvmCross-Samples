<local:BaseView x:Class="XPlatformMenus.WPF.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:MvvmCross.Wpf.Views;assembly=MvvmCross.Wpf"
             xmlns:local="clr-namespace:XPlatformMenus.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel Margin="10">
            <!--<TextBox Text="{Binding Username}" PlaceholderText="User name..." />-->
            <TextBox Margin="1" Text="{Binding Username}" />
            <!--<PasswordBox Password="{Binding Password}" PlaceholderText="Password..." />-->
            <PasswordBox Margin="1" />
            <Button Margin="1" Content="Login" Command="{Binding LoginCommand}" />
            <!--<ProgressBar IsIndeterminate="True" />-->
        </StackPanel>
    </Grid>
</local:BaseView>
